name: vitamin_of_the_day
description: Vitamin of the Day

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  hexcolor: ^2.0.3
  google_fonts: ^2.0.0
  bloc: ^6.1.0
  flutter_bloc: ^6.1.0
  equatable: ^1.2.5
  provider: ^4.0.0

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
